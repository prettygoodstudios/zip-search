require "spec_helper"

RSpec.describe Zip::Search do
  it "has a version number" do
    expect(Zip::Search::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
